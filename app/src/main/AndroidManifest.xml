<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:name=".core.ChimpApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/chimp_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/chimp_logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChIMP"
        tools:targetApi="31">
        <activity
            android:name=".core.activity.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.ChIMP">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".core.activity.AuthenticationActivity"
            android:exported="true"
            android:theme="@style/Theme.ChIMP">
        </activity>
    </application>

</manifest>